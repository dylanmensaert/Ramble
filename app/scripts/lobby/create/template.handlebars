<form class="form-horizontal">

    {{view view.input}}

    <div class="control-group">
        <div class="controls">
            <button class="btn btn-primary"
            {{action "create" this}}>
            Create
            </button>
            {{#linkTo "lobby.list" classNames="btn"}}
            Cancel
            {{/linkTo}}
        </div>
    </div>
</form>
