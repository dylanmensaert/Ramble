<div>
    <p>
        Are you sure you want to delete lobby <strong>{{title}}</strong>?
    </p>
</div>
<div>
    <button class="btn btn-danger"
    {{action "delete" this}}>
    Delete
    </button>
    {{#linkTo "lobby.index" classNames="btn"}}
    Cancel
    {{/linkTo}}
</div>
