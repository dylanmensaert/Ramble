<div>
    <p>
        Are you sure you want to delete account <strong>{{username}}</strong>?
    </p>
</div>
<div>
    <button class="btn btn-danger"
    {{action "doDelete" this}}>
    Delete
    </button>
    {{#linkTo "player.index" classNames="btn"}}
    Cancel
    {{/linkTo}}
</div>
