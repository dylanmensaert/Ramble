{{#linkTo "player.index" this}}
    {{id}} - {{username}}

    {{#if controller.isOwnerOfLobby}}
        <button {{action "kick" this bubbles=false}} class="close pull-right">
            &times;
        </button>
    {{/if}}
{{/linkTo}}
